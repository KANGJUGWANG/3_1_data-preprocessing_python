"""
시험 정보 정리
컴퓨터 사용 x 
어려운 함수 x
손코딩 베이스
"""
"""
+ 있는 열을 내용을 추가 해야함

이전 내용 정리(중요내용! +동영상 시청후 추가)
 - 가상환경 셋 이유
 : 패키지 버전 충돌문제를 해결하기 위해서 가상환경으로 지역을 구분하여 패키지 버전을 관리한다
 - 가상환경 설치
 1. cmd 열기
 2.1 conda create -n 가상환경이름 python = 파이썬 버전 (가상환경 생성)
 2.2 가상환경이 있다면 conda evn list (가상환경 리스트 확인)
 3. conda activaet 가상환경 (가상환경 접속)
 4. pip install jupyter notebook(주피터 노트북 설치 : 주피터 노트북 사용을 위해서)
 5. pip install ipykernel(가상환경과 연결하기 위해서 +?(뭐랑 뭐가 연결되는가))
 
 데이터 구조
    단일값 : 스칼라
    복수타입 & 복수값 : 리스트
    단일타입 & 복수값 : 배열
    
 데이터 타입
     숫자형(integer) : 1,1.1, 0 ,1
     논리형(Boolean) : 참 거짓
     문자형(char) : 문자열 
 타입 확인
 type(x)
 
 리스트 생성 
 x = [1,3,44,"aa"]
 
 배열 생성
     배열은 numpy로 관리됨 
     x = np.array(리스트)
 
 차원의 쉐입
    +시각적 판단 
    +함수로 판단 
    
 문자열 인덱싱
 x[시작위치: 직전위치]
 ex) x[-n : ] = 역순의 위치에서 시작

 문자열 출력 
 print(f"{x}{y} - x,y출력")
 print("{}{} - x,y 출력",format(x,y))
 +% 확인 
 
 + replace()
 
 리스트 연산
    리스트는 단일값에 대하여 연산이 가능하다
    x.index(",") <- 리스트의 ','가 나오는 위치를 리턴하는 함수
    
 리스트 추가 
     x.append(y) <- 리스트의 말단에 y를 추가한다
    x.insert(위치, 값) <-지정위치에 값을 추가한다
 리스트 삭제 
     x.pop(y) <- y위치값 삭제
     
 배열 추가 
  x = np.append(y) <- x의 말단에 y를 추가한다
  
  배열 삭제 
  x = delete() +
  
  배열 합성
   +
    
 딕셔너리 
 +
  x = {키값 : 벨류값}
  x1 = {
      키 = []
      벨류 = []
  }
 
 딕셔너리 삭제
  +
  
 딕셔너리 출력 
    x[1] = 키값이 1인 밸류값 출력
     = 키값이 없는 값이면 오류
    x.get(1) = 키값이 1인 밸류값 출력
     = 키값이 없으면 None
     
 데이터 프레임 
  데이터 프레임은 pandas로 구별된다
  
  데이터 프레임 생성
  x= pd.DataFrame
  
  데이터 프레임 불러오기 
  pd.read_csv('파일 위치')
   = 헤더가 있다면 0, 없으면 None
   = 파일의 인코딩이 utp-8, cp949, 
   
 데이터프레임 확인함수 
 + pdf tail()을 사용함
"""
